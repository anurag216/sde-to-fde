import type { RoadmapWeek } from '../domain'

export function RoadmapView({ weeks, onBack, onProfile }: { weeks: RoadmapWeek[]; onBack: () => void; onProfile: () => void }) {
  return <main className="shell roadmap-shell"><div className="view-nav"><button className="back" onClick={onBack}>← Dashboard</button><button className="ghost" onClick={onProfile}>Inspect skill evidence</button></div><header className="view-header"><p className="eyebrow">FIRST 4 WEEKS</p><h1>Your next engineering reps</h1><p>This is generated from current evidence + career interests. It deliberately mixes real engineering missions with exactly two DSA reps per week, then resurfaces an early concept in week four.</p></header><div className="weeks">{weeks.map((week) => <section className="week-card" key={week.week}><div className="week-title"><span>WEEK {week.week}</span><h2>{week.theme}</h2></div><div className="roadmap-items">{week.items.map((item) => <article key={item.id}><div className="roadmap-meta"><span>{item.kind}</span><span>{item.minutes} min</span>{item.skills.map((skill) => <span key={skill}>{skill}</span>)}</div><h3>{item.title}</h3><p>{item.outcome}</p><small><b>Why now:</b> {item.why}</small></article>)}</div></section>)}</div></main>
}
